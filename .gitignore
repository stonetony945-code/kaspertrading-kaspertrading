node_modules/
screenshots/
scripts/current.pine
temp_*
*.log
discovery-log.json

# Personal trading rules: watchlist, account size and risk limits.
# Start from rules.example.json — see SETUP_GUIDE.md step 2.
rules.json

# Market snapshots written by scripts/snapshot.js — generated artefacts.
snapshots/

# Secrets: Telegram bot token and chat id. Never commit this.
# Copy .env.example and fill it in — see scripts/test-notify.js to verify.
.env
